import React from "react";
import Slider from "../Slider";

export default (
    <Slider
        inputType="number"
        value={50}
        min={0}
        max={100}
        step={1}
        stepMuliplier={4}
        labelText="Slider Label"
        uxpId="Slider0"
    />
);
