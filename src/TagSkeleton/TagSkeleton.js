import React from "react";
import { TagSkeleton as TagSkeletonComponent } from "carbon-components-react";

function TagSkeleton(props) {
    return (
        <TagSkeletonComponent {...props} />
    );
}

export default TagSkeleton;
