import React from "react";
import { CheckboxSkeleton as CheckboxSkeletonComponent } from "carbon-components-react";

function CheckboxSkeleton(props) {
    return (
        <CheckboxSkeletonComponent {...props} />
    );
}

export default CheckboxSkeleton;
