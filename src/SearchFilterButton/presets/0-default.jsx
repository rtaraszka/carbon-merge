import React from "react";
import SearchFilterButton from "../SearchFilterButton";

export default (
    <SearchFilterButton
      label="Filter"
      labelText="Filter"
      uxpId="SearchFilterButton0"
    />
);
