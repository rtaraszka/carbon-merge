import React from "react";
import FileUploader from "../FileUploader";

export default (
    <FileUploader
        className="bob"
        labelText="Add files"
        buttonLabel="Upload Files"
        buttonKind="primary"
        multiple
        tabIndex={0}
        uxpId="FileUploader0"
    />
);
